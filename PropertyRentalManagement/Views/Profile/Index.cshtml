
@{
    ViewData["Title"] = "Index";
    var currentUser = ViewData["currentUser"];
    var currentClaim = ViewData["currentClaim"];
}

<h2>CurrentUser:</h2>@currentUser
<h2>Current Claim:</h2>@currentClaim
